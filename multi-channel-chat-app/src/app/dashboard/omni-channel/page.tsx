
// ----------------------------------------------------------------------

import { OmniChannelListView } from "@/sections/omni-channel/view";

export const metadata = {
  title: 'Danh sách trang',
};

export default function OmniChannelPage() {
  return <OmniChannelListView />;
}